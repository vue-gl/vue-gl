{
  "devDependencies": {
    "babel-minify": "^0.2.0",
    "rollup": "^0.49.2",
    "selenium-webdriver": "^3.5.0"
  },
  "scripts": {
    "test": "node test/index.js",
    "build": "rollup src/index.js -o docs/js/vue-gl.js -f umd --name VueGL --globals $(pwd)/three.js:THREE && mkdir -p dist && babel-minify docs/js/vue-gl.js --out-file dist/vue-gl.min.js"
  }
}
