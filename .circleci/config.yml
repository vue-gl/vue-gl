version: 2
jobs:
    build:
        docker:
            - image: circleci/node:latest-browsers
        steps:
            - checkout
            - run:
                name: Run test
                command:
                    xvfb-run firefox --headless file:///$(pwd)/test/index.html
