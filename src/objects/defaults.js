import {
  BufferGeometry, LineBasicMaterial, MeshBasicMaterial, PointsMaterial, SpriteMaterial,
} from 'three';

export const defaultGeometry = new BufferGeometry();
export const defaultMeshMaterial = new MeshBasicMaterial();
export const defaultLineMaterial = new LineBasicMaterial();
export const defaultPointsMaterial = new PointsMaterial();
export const defaultSpriteMaterial = new SpriteMaterial();
