import {
  AlphaFormat as alpha, RGBFormat as rgb, RGBAFormat as rgba, LuminanceFormat as luminance,
  LuminanceAlphaFormat as luminanceAlpha, RGBEFormat as rgbe, DepthFormat as depth,
  DepthStencilFormat as depthStencil,
} from 'three';

export default {
  alpha, rgb, rgba, luminance, luminanceAlpha, rgbe, depth, depthStencil,
};
